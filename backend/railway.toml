[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
# Sin startCommand: se usa el CMD del Dockerfile (shell-form) que SI expande $PORT.
# Railway no expande variables en el startCommand del toml.
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
