[pytest]
# Agrega el directorio backend/ (rootdir) a sys.path para que `import app`
# funcione al invocar `pytest backend/tests` desde la raíz del repo (CI),
# sin depender de `python -m pytest`.
pythonpath = .
testpaths = tests
